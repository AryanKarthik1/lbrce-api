spring.application.name=lbrce-api

# --- Aiven PostgreSQL (TLS required) ---
spring.datasource.url=jdbc:postgresql://pg-20515ae4-lbrce-db.f.aivencloud.com:21045/defaultdb?sslmode=require
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_OuxBoEnPsxHYiyW1Gk0

# Pooling (Aiven limit ~20 connections)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# JPA / Hibernate
# For first run while schema is empty you can use 'update' (or 'create' if you want a fresh DB each start).
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.open-in-view=false